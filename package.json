{
  "name": "test-orchestrator",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --enable-source-maps dist/index.js",
    "plan": "npm run build && node dist/index.js plan",
    "init-tests": "npm run build && node dist/index.js init-tests",
    "coverage": "npm run build && node dist/index.js coverage"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "commander": "^12.1.0",
    "fast-glob": "^3.3.2",
    "js-yaml": "^4.1.0",
    "minimatch": "^9.0.5"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/minimatch": "^5.1.2",
    "typescript": "^5.6.3"
  }
}
